# V2 implementation sequence

## Status

V1 is frozen as `v1.0.0`.
All V2 work happens on `v2-review-roadmap`.

## Core direction

V2 reframes the page from a broad spatial demonstrator into an argumentative map essay.

Central thesis:
The limiting factor in peatland transition is often not wet cultivation itself, but the downstream value chain: processing, buyers, standards and reliable volumes.

## Implementation order

### B130 — Engpass climax graphic
Build the downstream bottleneck graphic as responsive SVG/HTML.
No map logic changes.

### B131 — Narrative scaffold S0-S8
Restructure the page around the V2 storyboard:
expected climate framing → frame mismatch during zoom → downstream bottleneck.

### B132 — Statement titles
Rewrite section and graphic titles as claims, not topic labels.

### B133 — Scope and method boxes
Add scope box, method-in-brief, source-line pattern and institutional disclaimer draft.

### B134 — Map redesign pass
Unify map language, strengthen data-layer contrast, reduce visual noise, emphasize the Oberschwaben intersection.

### B135 — Mobile and accessibility
Mobile-first layout, responsive matrix, reduced motion, contrast and alt-text QA.
